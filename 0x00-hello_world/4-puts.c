#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
    puts("Programming is like building a multilingual puzzle");
    puts(""); // print an extra newline character
    return (0);
}
